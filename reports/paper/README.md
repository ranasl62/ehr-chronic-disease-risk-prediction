# Moved: paper verification outputs

Academic paper experiment outputs live under the local-only package (gitignored):

**`research-paper/reports/`**

```bash
make -C research-paper paper-quick
```

This directory is a path redirect only so old bookmarks are not a dead end.
Do **not** regenerate metrics here. Core framework reports stay in `reports/` (evaluation, runs, SHAP, etc.).
